<section id="hero" style="background:#f8fafc; padding:40px 0;">
  <div class="container" style="padding:0 24px;">
    <div class="hero-grid">

      <!-- Text links auf Desktop, oben auf Mobile -->
      <div class="hero-text">
        <p style="color:#2563eb; font-weight:500; margin:0;">Individuelles Laufcoaching</p>
        <h1 style="margin-top:16px; font-size:36px; line-height:1.2;">Lauftraining, das sich an dein Leben anpasst</h1>
        <p style="margin-top:16px; color:#475569;">Persönliches 1:1-Coaching und individuelle Trainingsplanung für ambitionierte Läufer:innen.</p>
        <a href="#contact" style="margin-top:24px; display:inline-block; background:#2563eb; color:white; padding:12px 24px; border-radius:8px; font-weight:500; text-decoration:none;">Kennenlerngespräch</a>
      </div>

      <!-- Bild rechts auf Desktop, unten auf Mobile -->
      <div class="hero-image">
        <img src="/hero.jpg" alt="Nils beim Laufen" />
      </div>

    </div>
  </div>

  <style>
    .hero-grid {
      display:grid;
      grid-template-columns: 1fr minmax(0, 380px); /* Text flexibel, Bild max 380px */
      align-items:center;
      gap:48px;
      max-width:1100px;
      margin:0 auto;
    }

    .hero-image img {
      display:block;
      width:100%;
      max-width:380px;
      height:auto;
      border-radius:12px;
      object-fit:cover;
    }

    /* Mobile: Text oben, Bild unten */
    @media(max-width:768px){
      .hero-grid {
        display:flex;
        flex-direction:column;
        gap:32px;
        padding:0 24px;
      }

      .hero-text {
        text-align:center;
      }

      .hero-image img {
        max-width:90%;
        margin:0 auto;
      }
    }
  </style>
</section>