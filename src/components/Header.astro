<header style="background:white; padding:8px 16px;">
  <div class="header-container">

    <!-- Logo + Text -->
    <div class="logo-text">
      <img src="/logo.png" alt="Logo" />
      <span>Nils Endurance Lab</span>
    </div>

    <!-- Navigation (nur Desktop) -->
    <nav class="desktop-nav">
      <a href="#about">Über mich</a>
      <a href="#whycoaching">Warum Coaching</a>
      <a href="#offers">Angebote</a>
      <a href="#faq">FAQ</a>
    </nav>

    <!-- Kontakt Button -->
    <a href="#contact" class="cta">Kontakt</a>

  </div>
</header>

<style>
.header-container {
  max-width:1100px;
  margin:0 auto;
  display:flex;
  justify-content:space-between;
  align-items:center;
  gap:16px;
}

.logo-text {
  display:flex;
  align-items:center;
  gap:12px;
  flex-shrink:1; /* erlaubt Text zu schrumpfen */
  min-width:0;
}

.logo-text img {
  height:48px;
  flex-shrink:0;
}

.logo-text span {
  font-family:'Space Grotesk', sans-serif;
  font-weight:700;
  font-size:1.2rem;
  color:#2563eb;
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}

.desktop-nav {
  display:flex;
  gap:24px;
}

.desktop-nav a {
  color:var(--text-primary);
  text-decoration:none;
}

.cta {
  padding:8px 16px;
  background:var(--accent);
  color:white;
  border-radius:8px;
  font-weight:500;
  text-decoration:none;
  flex-shrink:0; /* Button schrumpft nicht */
}

/* Mobile: Menüpunkte ausblenden, alles in einer Reihe */
@media(max-width:768px){
  .desktop-nav {
    display:none;
  }

  .logo-text span {
    font-size:1rem;
  }

  .cta {
    padding:6px 12px;
    font-size:0.9rem;
  }
}
</style>