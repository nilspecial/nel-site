<section id="contact" style="background:white; padding:40px 24px;">
  <div class="container" style="max-width:640px; margin:0 auto;">
    
    <h2 class="contact-title">
      Starte dein Training
    </h2>

    <p class="contact-intro">
      Schreib mir ein paar Zeilen zu dir, deinem aktuellen Trainingsstand
      und deinen Zielen.
    </p>

    <!-- Ablauf / Vertrauen -->
    <div class="contact-info">
      <strong>So geht es weiter:</strong>
      <ol>
        <li>Du sendest mir deine Anfrage</li>
        <li>Ich melde mich persönlich bei dir zurück</li>
        <li>Wir klären gemeinsam, welches Coaching für dich passt</li>
      </ol>
      <p>💡 Unverbindlich & ohne Verpflichtung.</p>
    </div>

    <!-- Formular -->
    <form
      action="https://formspree.io/f/mnqxyzab"
      method="POST"
      class="contact-form"
    >
      <input type="text" name="name" placeholder="Dein Name" required />
      <input type="email" name="email" placeholder="Deine E-Mail-Adresse" required />
      <textarea name="message" placeholder="Kurz zu dir: Laufniveau, Ziele, Wettkämpfe, Zeit pro Woche …" rows="5" required></textarea>
      <button type="submit">Unverbindliche Anfrage senden</button>
    </form>
  </div>

  <style>
    /* Hauptüberschrift */
    .contact-title {
      font-size: 32px;
      font-weight: 700;
      color: #2563eb;
      text-align: center;
      margin-bottom: 24px;
    }

    /* Intro-Text */
    .contact-intro {
      text-align: center;
      color: #475569;
      font-size: 16px;
      line-height: 1.6;
      margin-bottom: 32px;
    }

    /* Ablauf / Vertrauen */
    .contact-info {
      background: #f8fafc;
      border-radius: 8px;
      padding: 16px;
      margin-bottom: 32px;
      font-size: 14px;
      color: #475569;
    }

    .contact-info ol {
      padding-left: 18px;
      margin: 8px 0 0;
    }

    .contact-info p {
      margin-top: 8px;
    }

    /* Formular */
    .contact-form {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .contact-form input,
    .contact-form textarea {
      padding: 12px;
      border-radius: 6px;
      border: 1px solid #ccc;
      font-size: 16px;
      color: #1f2937;
    }

    .contact-form button {
      padding: 12px 24px;
      background: #2563eb;
      color: white;
      border: none;
      border-radius: 8px;
      font-weight: 500;
      cursor: pointer;
      transition: background 0.2s;
    }

    .contact-form button:hover {
      background: #1d4ed8;
    }

    /* Mobile-Anpassungen */
    @media(max-width:768px){
      .contact-title {
        font-size: 28px;
      }
      .contact-intro {
        font-size: 15px;
      }
      .contact-form input,
      .contact-form textarea {
        font-size: 15px;
      }
    }
  </style>
</section>